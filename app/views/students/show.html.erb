<p id="notice"><%= notice %></p>

<%= link_to 'Edit', edit_student_path(@student) %> |
<%= link_to 'Back', students_path %>
<%= link_to 'New Quiz Result', new_student_quiz_result_path(student_id: @student.student_id) %>
<%= @student.category %>
<% @quiz_results.each do |r| %>
  <p> <%= r.created_at %> </p>
<% end %>
